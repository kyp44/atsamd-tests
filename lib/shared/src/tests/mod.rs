// TODO: Once the PR gets merged, this will not longer need to be feature-gated
#[cfg(feature = "hal-rtc-test")]
mod hal_rtc;

// TODO: Once the PR gets merged, this will not longer need to be feature-gated
#[cfg(feature = "hal-delay-ns-test")]
mod hal_delay_ns;
